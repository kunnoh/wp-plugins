{
  "name": "wp-env-plugins",
  "version": "1.0.0",
  "devDependencies": {
    "@wordpress/env": "^10.25.0"
  },
  "scripts": {
    "start": "wp-env start",
    "deploy": "cd listings && npm run build && cd .. && wp-env stop && wp-env start"
  }
}
