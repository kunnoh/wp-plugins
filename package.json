{
  "name": "wp-env-plugins",
  "version": "1.0.0",
  "devDependencies": {
    "@wordpress/env": "^10.25.0"
  },
  "scripts": {
    "start": "cd listings && npm run build && cd .. && wp-env stop && wp-env start --update",
    "start:listing": "cd listings && npm run build && cd .. && wp-env stop && wp-env start --update",
    "start:contact": "cd contact && npm run build && cd .. && wp-env stop && wp-env start --update"
  }
}
